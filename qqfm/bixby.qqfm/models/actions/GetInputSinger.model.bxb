action (GetInputSinger) {
  type (Search)
  description (输入主播名字时获取的提示列表)
  collect {
    input (search_word) {
      type (search_word)
      min(Required)
      max(One)
    }
  }
  output (Singer)
}
